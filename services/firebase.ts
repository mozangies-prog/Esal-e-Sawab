
/**
 * FIREBASE REMOVED: 
 * This application now exclusively uses local storage for a zero-config, private experience.
 * Shared cloud features are disabled.
 */
export const isFirebaseConfigured = false;
export const db = null;
